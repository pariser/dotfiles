[user]
	name = Andrew Pariser
	email = pariser@gmail.com

[core]
	excludesfile = /Users/pariser/.gitignore

[color]
	diff = auto
	status = auto
	branch = auto

[alias]
	svn-diff = !git-svn-diff
	permission-reset = "!git-diff -p | grep -E '^(diff|old mode|new mode)' | sed -e 's/^old/NEW/;s/^new/old/;s/^NEW/new/' | git apply"

